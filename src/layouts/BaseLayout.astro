---
import "../styles/global.css";

interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const ogImage = new URL("/og-image.png", Astro.site);

const jsonLdSoftware = {
  "@context": "https://schema.org",
  "@type": "SoftwareApplication",
  name: "RepairKeeper",
  applicationCategory: "BusinessApplication",
  operatingSystem: "Web",
  description:
    "Repair shop management platform for phone, electronics, and computer repair businesses. Multi-device ticket tracking, intelligent auto-status, quoting, invoicing, inventory management, trade-in and refurbishment, online booking, customer self-service, and team management.",
  url: "https://repairkeeper.co.uk",
  screenshot: "https://repairkeeper.co.uk/dashboard.png",
  author: {
    "@type": "Organization",
    name: "RepairKeeper",
  },
  offers: [
    {
      "@type": "Offer",
      name: "Solo",
      price: "12",
      priceCurrency: "GBP",
      description: "75 tickets/mo, 1 team member. Core ticketing, invoicing, customer tracking, Stripe Connect, and custom SMTP. Starts with a 14-day free trial.",
    },
    {
      "@type": "Offer",
      name: "Lite",
      price: "20",
      priceCurrency: "GBP",
      description: "150 tickets/mo, 1 team member. Adds trade-in system, online booking, and product catalogue. Starts with a 14-day free trial.",
    },
    {
      "@type": "Offer",
      name: "Standard",
      price: "30",
      priceCurrency: "GBP",
      description: "Unlimited tickets, 5 team members. Adds RBAC, audit log, reporting, device photos, branding removal, and webhooks & integrations. Starts with a 14-day free trial.",
    },
    {
      "@type": "Offer",
      name: "Pro",
      price: "55",
      priceCurrency: "GBP",
      description: "Unlimited tickets, 15 team members. Adds note file attachments on top of the Standard feature set. Starts with a 14-day free trial.",
    },
  ],
};

const jsonLdOrganization = {
  "@context": "https://schema.org",
  "@type": "Organization",
  name: "RepairKeeper",
  url: "https://repairkeeper.co.uk",
  logo: "https://repairkeeper.co.uk/favicon.svg",
  contactPoint: {
    "@type": "ContactPoint",
    email: "support@repairkeeper.co.uk",
    contactType: "customer support",
  },
  sameAs: [],
};

const jsonLdWebSite = {
  "@context": "https://schema.org",
  "@type": "WebSite",
  name: "RepairKeeper",
  url: "https://repairkeeper.co.uk",
  description: "Repair shop management software for UK repair businesses.",
};

const jsonLdBreadcrumb = {
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  itemListElement: [
    {
      "@type": "ListItem",
      position: 1,
      name: "Home",
      item: "https://repairkeeper.co.uk",
    },
  ],
};
---

<!doctype html>
<html lang="en-GB" class="antialiased">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <meta name="author" content="RepairKeeper" />

    <!-- SEO -->
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalURL} />
    <meta name="robots" content="index, follow, max-snippet:-1, max-image-preview:large, max-video-preview:-1" />
    <meta name="theme-color" content="#4F46E5" />
    <meta name="keywords" content="repair shop software, repair management, phone repair software, electronics repair, ticket management, repair shop POS, repair tracking, UK repair software, multi-device tickets, repair invoicing" />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content="RepairKeeper — repair shop management software dashboard showing tickets, inventory, and customer tracking" />
    <meta property="og:locale" content="en_GB" />
    <meta property="og:site_name" content="RepairKeeper" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImage} />
    <meta name="twitter:image:alt" content="RepairKeeper — repair shop management software dashboard showing tickets, inventory, and customer tracking" />

    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="apple-touch-icon" href="/og-image.png" />

    <!-- Preconnect to fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap"
      rel="stylesheet"
    />

    <!-- JSON-LD -->
    <script type="application/ld+json" set:html={JSON.stringify(jsonLdSoftware)} />
    <script type="application/ld+json" set:html={JSON.stringify(jsonLdOrganization)} />
    <script type="application/ld+json" set:html={JSON.stringify(jsonLdWebSite)} />
    <script type="application/ld+json" set:html={JSON.stringify(jsonLdBreadcrumb)} />
  </head>
  <body class="min-h-screen bg-white text-dark">
    <!-- Skip navigation -->
    <a
      href="#main-content"
      class="sr-only focus:not-sr-only focus:fixed focus:left-4 focus:top-4 focus:z-[100] focus:rounded-lg focus:bg-primary focus:px-4 focus:py-2 focus:text-white focus:shadow-lg"
    >
      Skip to main content
    </a>

    <slot />

    <!-- Scroll reveal + mobile menu -->
    <script is:inline>
      document.addEventListener("DOMContentLoaded", () => {
        // Scroll reveal with IntersectionObserver
        const reveals = document.querySelectorAll(".reveal");
        if (reveals.length > 0) {
          const observer = new IntersectionObserver(
            (entries) => {
              entries.forEach((entry) => {
                if (entry.isIntersecting) {
                  entry.target.classList.add("revealed");
                  observer.unobserve(entry.target);
                }
              });
            },
            { threshold: 0.1, rootMargin: "0px 0px -40px 0px" }
          );
          reveals.forEach((el) => observer.observe(el));
        }

        // Mobile menu toggle
        const menuBtn = document.getElementById("mobile-menu-btn");
        const mobileMenu = document.getElementById("mobile-menu");
        if (menuBtn && mobileMenu) {
          menuBtn.addEventListener("click", () => {
            const expanded = menuBtn.getAttribute("aria-expanded") === "true";
            menuBtn.setAttribute("aria-expanded", String(!expanded));
            mobileMenu.classList.toggle("hidden");
          });
          // Close menu when a link is clicked
          mobileMenu.querySelectorAll("a").forEach((link) => {
            link.addEventListener("click", () => {
              mobileMenu.classList.add("hidden");
              menuBtn.setAttribute("aria-expanded", "false");
            });
          });
        }
      });
    </script>
  </body>
</html>
